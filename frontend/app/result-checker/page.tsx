'use client';
import { FormEvent, useState } from 'react';
export default function ResultChecker(){const [submitted,setSubmitted]=useState(false);function submit(e:FormEvent){e.preventDefault();setSubmitted(true)}return <section className="section"><div className="container narrow"><p className="eyebrow">SECURE RESULT CHECKER</p><h1>Check a student result</h1><p className="lead">Enter the student examination number, session and term. Only published results will be returned by the backend.</p><form className="form-card" onSubmit={submit}><label>Student / Examination Number<input name="studentNumber" required placeholder="e.g. LBS/2026/001" /></label><label>Session<select name="session" required><option value="">Select session</option><option>2025/2026</option><option>2026/2027</option></select></label><label>Term<select name="term" required><option value="">Select term</option><option>First Term</option><option>Second Term</option><option>Third Term</option></select></label><label>Result PIN (if required)<input name="pin" inputMode="numeric" placeholder="Optional PIN" /></label><button className="button primary" type="submit">Check result</button>{submitted&&<div className="notice success"><b>Secure lookup ready.</b><span>The form is prepared for the backend result-checking endpoint. No student data is displayed until a published result is verified server-side.</span></div>}</form></div></section>}
